import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// Standard Vite + Vue configuration
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173, // frontend will run here
  },
})
